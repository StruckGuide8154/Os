Base surface container for panels, tiles and list items. Add `interactive` for hover lift, `glow` for the featured neon edge.

```jsx
<Card padding="lg" interactive onClick={open}>…</Card>
<Card glow>Selected</Card>
```
