Text field with inset styling; focus lights the neon ring. Add `icon` for a leading glyph, `invalid` for the error state.

```jsx
<Input placeholder="Search files…" icon={<i data-lucide="search" />} />
<Input type="password" defaultValue="hunter2" invalid />
```
