module "unknown_cap_forbidden";

# Negative fixture (nhl-beyond-zero-trust-todo.md "compiler tests proving
# unknown unsafe declarations are hard errors"). The unsafe-capability namespace
# is a closed allowlist (_VALID_UNSAFE_CAPS): an unrecognised authority must be
# rejected at parse time, never silently treated as "no capability" (which would
# let a typo'd or invented authority name quietly disable a gate). This module
# declares a capability that does not exist. Compile error expected:
# "unsafe not_a_real_authority: unknown capability".
unsafe not_a_real_authority;

global nxh_test_unknown_cap_forbidden;

fn nxh_test_unknown_cap_forbidden() {
    return 0;
}
